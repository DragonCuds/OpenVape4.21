org.gradle.jvmargs=-Xmx1g -Dfile.encoding=UTF-8
targetRelease=8

# 强制使用 Visual Studio 2026 生成器（解决 NMake x64 平台问题）
cmake.generator=Visual Studio 18 2026
cmake.platform=x64

# 如果不使用 x64，可以尝试：
# cmake.generator=NMake Makefiles
# 但这样会失去 x64 平台支持
